# Archived

This topic has been superseded by two new topics as of v1.0-r8:

- `daemon-architecture/` — Module decomposition, state tree, type definitions,
  integration boundaries, transport design
- `daemon-behavior/` — Policies, lifecycles, state machines, event handling,
  startup/shutdown

The last revision under this topic is `draft/v1.0-r7/`. All draft directories
are retained as historical record.
